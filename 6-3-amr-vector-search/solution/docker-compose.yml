services:
  redis:
    image: redis/redis-stack:latest
    ports:
      - "6379:6379"   # Redis protocol
      - "8001:8001"   # RedisInsight web UI
    volumes:
      - redis-data:/data

volumes:
  redis-data:
